
using System.Threading.Channels;
using DesignPatterns.Adapter;
Console.WriteLine();


#region Adapter
/*
// Generate a new Hole with a radius of 5.
RoundHole hole = new RoundHole(5);


// Generate a new RoundPeg with a radius of 6.
RoundPeg rpeg = new RoundPeg(6);


// Generate a new SquarePeg with a width of 6.
SquarePeg speg = new SquarePeg(6);

// Determine if the RoundPeg fits the RoundHole.
Console.WriteLine(hole.FitsPeg(rpeg));


// Determine if the SquarePeg with is adapter fits the RoundHole.
Console.WriteLine(hole.FitsPeg(new SquarePegAdapter(speg)));
*/
#endregion


